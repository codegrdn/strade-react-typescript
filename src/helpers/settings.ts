export const settingsForLanding = (window: any) => {
    // window?.document.querySelector('html')?.className = 'page';
    // window?.document.querySelector('body')?.className  = 'page__body';
}

export const settingsForMarket = (window: any) => {
    // window?.document?.querySelector('html')?.className = 'page theme';
    // window?.document?.querySelector('body')?.className  = 'page__body dashboard';
}